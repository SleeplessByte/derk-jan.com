<section data-component="PreviewTinyClockVariations" class="preview">
  <div class="digit">
    <div
      style="--angle-hour: 0deg; --angle-minute: 180deg; animation: rotate 4s infinite steps(4);"
    >
    </div>
    <div
      style="--angle-hour: 0deg; --angle-minute: 90deg; animation: rotate 4s infinite steps(4);"
    >
    </div>
    <div style="--angle-hour: 135deg; --angle-minute: 135deg;"></div>
  </div>
</section>

<style>
  @keyframes rotate {
    from {
      transform: rotate(0deg);
    }

    to {
      transform: rotate(360deg);
    }
  }

  .preview {
    outline: 1px dotted light-dark(var(--color-zinc-800), var(--color-zinc-200));
    background-color: light-dark(var(--color-white), var(--color-zinc-950));

    max-width: 30rem;
    margin-left: auto;
    margin-right: auto;

    padding: 1rem;

    & > .digit {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      grid-template-rows: repeat(1, 1fr);

      gap: 0.5rem;
    }
  }
</style>
